<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.mateuszholik.passwordgenerator.ui.generatepassword.GeneratePasswordViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/primary"
        android:orientation="vertical">

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3" />

        <com.airbnb.lottie.LottieAnimationView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_gravity="center_horizontal"
            app:lottie_autoPlay="true"
            app:lottie_loop="false"
            app:lottie_rawRes="@raw/add_animation" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            style="@style/DefaultText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/margin_16"
            android:text="@string/generate_password_length" />

        <com.google.android.material.slider.Slider
            android:id="@+id/slider"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/margin_8"
            android:layout_marginTop="@dimen/margin_16"
            android:stepSize="1.0"
            android:value="8.0"
            android:valueFrom="8.0"
            android:valueTo="20.0"
            app:onValueChangeListener="@{(value) -> viewModel.onValueChanged(value)}"
            app:thumbColor="@color/secondary_dark"
            app:trackColor="@color/secondary_dark" />

        <Button
            android:id="@+id/generatePassword"
            style="@style/Button.Dark"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/margin_16"
            android:onClick="@{() -> viewModel.createPassword()}"
            android:text="@string/generate_create_password" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="2" />


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_marginHorizontal="@dimen/margin_16"
            android:background="@drawable/rounded_dark_background"
            android:orientation="horizontal"
            app:isVisible="@{!viewModel.generatedPassword.empty}">

            <TextView
                android:id="@+id/generatedPasswordTV"
                style="@style/DefaultText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginStart="@dimen/margin_16"
                android:layout_weight="1"
                android:text="@{viewModel.generatedPassword}" />

            <ImageButton
                android:id="@+id/goToSavePasswordScreenButton"
                android:layout_width="55dp"
                android:layout_height="55dp"
                android:layout_marginHorizontal="@dimen/margin_8"
                android:background="@null"
                android:contentDescription="@string/generate_password_save_button_description"
                android:scaleType="center"
                android:src="@drawable/ic_save" />

        </LinearLayout>

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3" />

    </LinearLayout>
</layout>